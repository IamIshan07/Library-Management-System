# In local development, this can stay unset — api.js falls back to
# http://localhost:5000/api automatically.
#
# In production (Netlify / Vercel), set this to your deployed Render
# backend URL, for example:
# VITE_API_URL=https://stackroom-backend.onrender.com/api
VITE_API_URL=
